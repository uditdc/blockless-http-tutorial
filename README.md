# Blockless Http Example

This is a sample `Blockless` application. To use :

- clone repository
- run `npm install`
- run `bls function build && bls function invoke`

Get the `bls` command line utility here: https://github.com/blocklessnetwork/cli